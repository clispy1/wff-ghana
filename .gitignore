node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example
tsconfig.tsbuildinfo

# Agent skills installed locally via `npx skills add` (e.g. next-dev-loop).
# Tooling, not app code — install on demand rather than committing.
.agents/
.claude/skills/
skills-lock.json
